spring.application.name=mmtspl-cloud-config-service

server.port=9006
#server.address=0.0.0.0
server.address=10.109.199.148

# urls taken from above application name which is similar to the property name like
# mmtspl-cloud-config-service-urls.properties under mmtspl-services-config-repo repository in git
spring.cloud.config.uri=http://${server.address}:30800 

#eureka.client.service-url.default-zone=http://${server.address}:8761/eureka

#spring.profiles.active=frontcontrollerurls
#management.security.enabled=false
#spring.cloud.bus.enabled=true
#management.endpoints.web.exposure.include=bus-refresh
